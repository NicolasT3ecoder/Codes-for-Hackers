getSelectionType = true;

RouterAccess = true;
Get:LocalRouter:(RTCPeerConnection).bind
RouterAccess = null;
Wait(0.5)
RouterAccess.LocalAccess._window = (bind(caller(s
)));
Get:LocalRouuter:(RTCPeerConnection).apply(bind(console.error(Script, console
)));
wait(1); 
 Element;
    encodeURI;
End;
 End;
  End;
   End;