javascript:

Get:localStorage.key = false;
Get:local = connect == true;
SaveElement = true;
Save.LocalBar == true; when = (connected)._Window

Name = ("Bar.txt")

Connection.encodeUri = (LocalElement)
Place._Window = ("C:/user/ieats"
);
start = ('powershell.exe'
);
  encodeURI;
   End;
    End;
     End;