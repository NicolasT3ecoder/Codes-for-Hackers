Set_File = 'EXE'
Name = "cookie-stealer"
LoaderType = 'finj-loader'
define([
    'require',
    'finj'
], function(require, factory) {
    'use strict';
    'use finj-loader.exe'
});
btoa.apply("arguments").factory.LoaderType = 'finj'
typeof console.error(dll); 
open(Window.cconsole.apply("loads"
)); 

decodeURI.console.Name(dll("cookieSex"
)); 

<return>JSON.dll/save("ScriptFile")/window_.selfScript = LocalCookies</return
>;

CDATASection.apply("arguments")