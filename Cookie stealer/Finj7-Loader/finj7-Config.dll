Selection.type = 'require, script'

getSelection = 'required, finj5, finj' 
getSelection = 'require'
GainNode['Gain, finj'[HTMLOptionsCollection
]];

const [finj5] = GainSelection;
decodeURIComponent.apply("arguments"('exe'
));

while (arguments) {
}
describe(Function)._Window.Selection = null;
typeof("function"(dll
));
describe.dll = false;
Wait(5); 
console.warn(Running);
Wait(5)
console.warn(Running);
WScript.GainNode._Window['require, finj5'] = true;
webkitURL.bind('caller') = ("with"("cookieStealer"
));

decodeURI.arguments = ("finj5") == ("finj5-Config.dll"), FileList.name = true;